{
  "key": "heart_rate",
  "name": "Heart Rate Monitor",
  "version": "1.0.0",
  "deviceType": "heart-rate",
  "platforms": ["linux", "darwin", "win32"],
  "capabilities": ["health.heart-rate", "health.rr-interval"],
  "configurationSchema": {
    "type": "object",
    "properties": {
      "backend": { "enum": ["ble", "serial"] },
      "serialPort": { "type": "string" },
      "serialBaud": { "type": "integer" },
      "deviceAddress": { "type": "string" }
    }
  },
  "channels": [
    {
      "key": "heart_rate",
      "name": "Heart rate",
      "modality": "health",
      "unit": "bpm",
      "sampleRate": 1,
      "configurationSchema": {}
    }
  ],
  "optionalDependency": "heart-rate",
  "mock": false
}
