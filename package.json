{
  "name": "the-scarline",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev": "./scarline start --dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
