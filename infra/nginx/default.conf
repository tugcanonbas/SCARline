server {
  listen 80;
  server_name _;
  absolute_redirect off;

  location = / {
    return 302 /admin/dashboard;
  }

  location /api/ {
    proxy_pass http://core-api:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }

  location /ws {
    proxy_pass http://core-api:8080/ws;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $host;
  }

  location /overlay/ {
    proxy_pass http://overlay-web:4000/;
    proxy_set_header Host $host;
  }

  location = /documentation {
    return 308 /docs/;
  }

  location /documentation/ {
    return 308 /docs/;
  }

  location /docs/ {
    proxy_pass http://docs:4040/;
    proxy_set_header Host $host;
  }

  location /rabbitmq/ {
    proxy_pass http://rabbitmq:15672/;
    proxy_set_header Host $host;
  }

  location ~ ^/(dashboard|startup|onboarding|login|user-studies|settings|documentation|researchers|session-logs)(/.*)?$ {
    return 308 /admin$request_uri;
  }

  location /admin/ {
    proxy_pass http://admin-panel:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }

  location / {
    return 404;
  }
}
